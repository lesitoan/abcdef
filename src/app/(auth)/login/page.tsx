import AuthScreen from '@/screens/authScreen';

export const metadata = {
  title: 'Đăng nhập - V-BENCH',
  description: 'Đăng nhập vào tài khoản V-BENCH',
};

export default function LoginPage() {
  return <AuthScreen />;
}
