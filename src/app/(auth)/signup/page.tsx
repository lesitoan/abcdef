import SignupForm from '@/screens/authScreen/components/SignupForm';

export const metadata = {
  title: 'Đăng ký - V-BENCH',
  description: 'Tạo tài khoản V-BENCH mới',
};

export default function SignupPage() {
  return <SignupForm />;
}
